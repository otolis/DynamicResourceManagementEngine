# Shared Packages

This directory will contain shared TypeScript types and utilities used by both the API and client.

## Planned Contents

- `types/` - Shared TypeScript interfaces and types
- `utils/` - Shared utility functions
- `constants/` - Shared constants and enums
