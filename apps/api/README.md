# NestJS API

This directory will contain the NestJS backend application.

## Setup (Phase 1)

The NestJS application will be initialized during Phase 1 with:
- Prisma ORM configuration
- JWT authentication module
- RBAC/ABAC security guards
- Meta-schema API endpoints
